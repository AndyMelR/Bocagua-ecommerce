{/*import React, {useContext} from "react";
import { Context } from "../store/appContext"; 



const CheckoutPage = () => {
  const { actions, store } = useContext(Context); // Access the cart items from the store

  return (
    <>
    <div>
      <h2>Checkout</h2>
      {store.cart.map((item) => (
        <div key={item.id}>
          <h3>{item.name}</h3>
          <p>Price: ${item.price}</p>
          <p>Quantity: {item.quantity}</p>
        </div>
      ))}
      
    </div>
    </>
  );
};


const Message = ({ message }) => (
  <section>
    <p>{message}</p>
  </section>
);

export {CheckoutPage, Message}*/}
